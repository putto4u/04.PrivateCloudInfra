APT 및 캐시 정리
Bash
sudo apt-get clean
sudo rm -rf /var/lib/apt/lists/*
머신 ID 초기화 (중요)
노드마다 고유한 ID를 가져야 하므로, 골든 이미지에서는 이 파일을 비워두어야 부팅 시 새로 생성됩니다.

Bash
sudo truncate -s 0 /etc/machine-id
sudo rm /var/lib/dbus/machine-id
sudo ln -s /etc/machine-id /var/lib/dbus/machine-id
